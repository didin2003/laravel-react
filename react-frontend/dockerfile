FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

# Force Vite preview to run on port 5000
EXPOSE 5000

CMD ["npm", "run", "preview", "--", "--host", "--port", "5000"]
